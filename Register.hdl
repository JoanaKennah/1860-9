CHIP Register {
    IN indata[4], load;
    OUT out[4];

    PARTS:
	Bit(indata[0]=indata[0],load=load,out=out[0]);
	Bit(indata[0]=indata[1],load=load,out=out[1]);
	Bit(indata[0]=indata[2],load=load,out=out[2]);
	Bit(indata[0]=indata[3],load=load,out=out[3]);

}