CHIP Register {
    IN indata[4], load;
    OUT out[4];

    PARTS:
	Bit(in=indata[0],load=load,out=out[0]);
	Bit(in=indata[1],load=load,out=out[1]);
	Bit(in=indata[2],load=load,out=out[2]);
	Bit(in=indata[3],load=load,out=out[3]);

}