CHIP Shift {
    IN in[4];
    OUT out[4], carryOut;

    PARTS:
    Or(a=false,b=in[3],out=out[2]);
	Or(a=false,b=in[2],out=out[1]);
	Or(a=false,b=in[1],out=out[0]);
	Or(a=false,b=in[0],out=carryOut);
}